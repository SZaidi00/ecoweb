# Data format

This document will describe the `EcosystemWeb` JSON format used by the files in `data/webs/` — node and edge fields, trophic levels, provenance/citation metadata, and validation rules. It lands in **Phase 1**, together with the JSON Schema and zod validators in `packages/schema`.
